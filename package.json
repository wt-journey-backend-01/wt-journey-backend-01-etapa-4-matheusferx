{
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "knex": "^3.1.0",
    "pg": "^8.16.3"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "npx knex migrate:latest",
    "migrate:rollback": "npx knex migrate:rollback",
    "seed": "npx knex seed:run",
    "db:reset": "docker-compose down -v && docker-compose up -d && npx knex migrate:rollback --all && npx knex migrate:latest && npx knex seed:run"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
